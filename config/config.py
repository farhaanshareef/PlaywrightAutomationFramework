class Config:
    # Application URL
    APP_URL = "https://demoqa.com/automation-practice-form"

    # Browser-related configurations (optional, for later use)
    BROWSER = "chromium"  # Options: chromium, firefox, webkit
    HEADLESS = False  # Run browser in headless mode